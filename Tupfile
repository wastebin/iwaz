include_rules

: foreach src/* |> !vs |> build/%b
: foreach build/*.hh | build/mpc/*.gch |> !hx -Lbuild |> {gchs}
: foreach build/*.cc | build/mpc/*.gch {gchs} |> !cx -Lbuild |> build/%B.o
: build/*.o build/mpc/libmpc.a |> !lx |> dist/iwaz
